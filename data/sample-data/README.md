# Sample Data

These files are small/sanitized samples for repository demonstration only.

Do not add production exports here. Large/generated data, Parquet files, checkpoints and secrets are ignored by `.gitignore`.
